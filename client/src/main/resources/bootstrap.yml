spring:
  application:
    name: mallInfo
  cloud:
    config:
      uri: http://${env.hostname.config:localhost}:8888
      profile: ${env.profile:dev}

encrypt:
  failOnError: false

logging:
  level: debug
  path: d:\\a.log
